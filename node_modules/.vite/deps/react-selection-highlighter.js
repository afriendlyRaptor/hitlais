import {
  require_client
} from "./chunk-6OZOVKEO.js";
import "./chunk-N35364TL.js";
import {
  require_jsx_runtime
} from "./chunk-N46KHF4R.js";
import {
  require_react
} from "./chunk-TAJOU4CS.js";
import {
  __commonJS,
  __toESM
} from "./chunk-PX6F3LHL.js";

// node_modules/get-document/index.js
var require_get_document = __commonJS({
  "node_modules/get-document/index.js"(exports, module) {
    module.exports = getDocument;
    var DOCUMENT_NODE = 9;
    function isDocument(d) {
      return d && d.nodeType === DOCUMENT_NODE;
    }
    function getDocument(node) {
      if (isDocument(node)) {
        return node;
      } else if (isDocument(node.ownerDocument)) {
        return node.ownerDocument;
      } else if (isDocument(node.document)) {
        return node.document;
      } else if (node.parentNode) {
        return getDocument(node.parentNode);
      } else if (node.commonAncestorContainer) {
        return getDocument(node.commonAncestorContainer);
      } else if (node.startContainer) {
        return getDocument(node.startContainer);
      } else if (node.anchorNode) {
        return getDocument(node.anchorNode);
      }
    }
  }
});

// node_modules/ancestors/index.js
var require_ancestors = __commonJS({
  "node_modules/ancestors/index.js"(exports, module) {
    module.exports = parents;
    function parents(node, filter) {
      var out = [];
      filter = filter || noop;
      do {
        out.push(node);
        node = node.parentNode;
      } while (node && node.tagName && filter(node));
      return out.slice(1);
    }
    function noop(n) {
      return true;
    }
  }
});

// node_modules/index-of/index.js
var require_index_of = __commonJS({
  "node_modules/index-of/index.js"(exports, module) {
    "use strict";
    module.exports = function indexOf(arr, ele, start) {
      start = start || 0;
      var idx = -1;
      if (arr == null) return idx;
      var len = arr.length;
      var i = start < 0 ? len + start : start;
      if (i >= arr.length) {
        return -1;
      }
      while (i < len) {
        if (arr[i] === ele) {
          return i;
        }
        i++;
      }
      return -1;
    };
  }
});

// node_modules/dom-seek/lib/index.js
var require_lib = __commonJS({
  "node_modules/dom-seek/lib/index.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports["default"] = seek;
    var _ancestors = require_ancestors();
    var _ancestors2 = _interopRequireDefault(_ancestors);
    var _indexOf = require_index_of();
    var _indexOf2 = _interopRequireDefault(_indexOf);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    var E_SHOW = "Argument 1 of seek must use filter NodeFilter.SHOW_TEXT.";
    var E_WHERE = "Argument 2 of seek must be a number or a Text Node.";
    var SHOW_TEXT = 4;
    var TEXT_NODE = 3;
    function seek(iter, where) {
      if (iter.whatToShow !== SHOW_TEXT) {
        throw new Error(E_SHOW);
      }
      var count = 0;
      var node = iter.referenceNode;
      var predicates = null;
      if (isNumber(where)) {
        predicates = {
          forward: function forward2() {
            return count < where;
          },
          backward: function backward2() {
            return count > where;
          }
        };
      } else if (isText(where)) {
        var forward = before(node, where) ? function() {
          return false;
        } : function() {
          return node !== where;
        };
        var backward = function backward2() {
          return node != where || !iter.pointerBeforeReferenceNode;
        };
        predicates = { forward, backward };
      } else {
        throw new Error(E_WHERE);
      }
      while (predicates.forward() && (node = iter.nextNode()) !== null) {
        count += node.nodeValue.length;
      }
      while (predicates.backward() && (node = iter.previousNode()) !== null) {
        count -= node.nodeValue.length;
      }
      return count;
    }
    function isNumber(n) {
      return !isNaN(parseInt(n)) && isFinite(n);
    }
    function isText(node) {
      return node.nodeType === TEXT_NODE;
    }
    function before(ref, node) {
      if (ref === node) return false;
      var common = null;
      var left = [ref].concat((0, _ancestors2["default"])(ref)).reverse();
      var right = [node].concat((0, _ancestors2["default"])(node)).reverse();
      while (left[0] === right[0]) {
        common = left.shift();
        right.shift();
      }
      left = left[0];
      right = right[0];
      var l = (0, _indexOf2["default"])(common.childNodes, left);
      var r = (0, _indexOf2["default"])(common.childNodes, right);
      return l > r;
    }
  }
});

// node_modules/dom-seek/index.js
var require_dom_seek = __commonJS({
  "node_modules/dom-seek/index.js"(exports, module) {
    module.exports = require_lib()["default"];
  }
});

// node_modules/simple-xpath-position/lib/dom-exception.js
var require_dom_exception = __commonJS({
  "node_modules/simple-xpath-position/lib/dom-exception.js"(exports) {
    "use strict";
    exports.__esModule = true;
    function _classCallCheck(instance, Constructor) {
      if (!(instance instanceof Constructor)) {
        throw new TypeError("Cannot call a class as a function");
      }
    }
    var DOMException = function DOMException2(message, name) {
      _classCallCheck(this, DOMException2);
      this.message = message;
      this.name = name;
      this.stack = new Error().stack;
    };
    exports["default"] = DOMException;
    DOMException.prototype = new Error();
    DOMException.prototype.toString = function() {
      return this.name + ": " + this.message;
    };
  }
});

// node_modules/simple-xpath-position/lib/xpath.js
var require_xpath = __commonJS({
  "node_modules/simple-xpath-position/lib/xpath.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.fromNode = fromNode;
    exports.toNode = toNode;
    var _getDocument = require_get_document();
    var _getDocument2 = _interopRequireDefault(_getDocument);
    var _domException = require_dom_exception();
    var _domException2 = _interopRequireDefault(_domException);
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    var FIRST_ORDERED_NODE_TYPE = 9;
    var HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
    function fromNode(node) {
      var root = arguments.length <= 1 || arguments[1] === void 0 ? null : arguments[1];
      if (node === void 0) {
        throw new Error('missing required parameter "node"');
      }
      root = root || (0, _getDocument2["default"])(node);
      var path = "/";
      while (node !== root) {
        if (!node) {
          var message = "The supplied node is not contained by the root node.";
          var name = "InvalidNodeTypeError";
          throw new _domException2["default"](message, name);
        }
        path = "/" + nodeName(node) + "[" + nodePosition(node) + "]" + path;
        node = node.parentNode;
      }
      return path.replace(/\/$/, "");
    }
    function toNode(path, root) {
      var resolver = arguments.length <= 2 || arguments[2] === void 0 ? null : arguments[2];
      if (path === void 0) {
        throw new Error('missing required parameter "path"');
      }
      if (root === void 0) {
        throw new Error('missing required parameter "root"');
      }
      var document2 = (0, _getDocument2["default"])(root);
      if (root !== document2) path = path.replace(/^\//, "./");
      var documentElement = document2.documentElement;
      if (resolver === null && documentElement.lookupNamespaceURI) {
        (function() {
          var defaultNS = documentElement.lookupNamespaceURI(null) || HTML_NAMESPACE;
          resolver = function resolver2(prefix) {
            var ns = { "_default_": defaultNS };
            return ns[prefix] || documentElement.lookupNamespaceURI(prefix);
          };
        })();
      }
      return resolve(path, root, resolver);
    }
    function nodeName(node) {
      switch (node.nodeName) {
        case "#text":
          return "text()";
        case "#comment":
          return "comment()";
        case "#cdata-section":
          return "cdata-section()";
        default:
          return node.nodeName.toLowerCase();
      }
    }
    function nodePosition(node) {
      var name = node.nodeName;
      var position = 1;
      while (node = node.previousSibling) {
        if (node.nodeName === name) position += 1;
      }
      return position;
    }
    function resolve(path, root, resolver) {
      try {
        var nspath = path.replace(/\/(?!\.)([^\/:\(]+)(?=\/|$)/g, "/_default_:$1");
        return platformResolve(nspath, root, resolver);
      } catch (err) {
        return fallbackResolve(path, root);
      }
    }
    function fallbackResolve(path, root) {
      var steps = path.split("/");
      var node = root;
      while (node) {
        var step = steps.shift();
        if (step === void 0) break;
        if (step === ".") continue;
        var _step$split = step.split(/[\[\]]/);
        var name = _step$split[0];
        var position = _step$split[1];
        name = name.replace("_default_:", "");
        position = position ? parseInt(position) : 1;
        node = findChild(node, name, position);
      }
      return node;
    }
    function platformResolve(path, root, resolver) {
      var document2 = (0, _getDocument2["default"])(root);
      var r = document2.evaluate(path, root, resolver, FIRST_ORDERED_NODE_TYPE, null);
      return r.singleNodeValue;
    }
    function findChild(node, name, position) {
      for (node = node.firstChild; node; node = node.nextSibling) {
        if (nodeName(node) === name && --position === 0) break;
      }
      return node;
    }
  }
});

// node_modules/simple-xpath-position/index.js
var require_simple_xpath_position = __commonJS({
  "node_modules/simple-xpath-position/index.js"(exports, module) {
    module.exports = require_xpath();
  }
});

// node_modules/xpath-range/lib/range.js
var require_range = __commonJS({
  "node_modules/xpath-range/lib/range.js"(exports) {
    "use strict";
    exports.__esModule = true;
    exports.fromRange = fromRange2;
    exports.toRange = toRange2;
    var _getDocument = require_get_document();
    var _getDocument2 = _interopRequireDefault(_getDocument);
    var _domSeek = require_dom_seek();
    var _domSeek2 = _interopRequireDefault(_domSeek);
    var _simpleXpathPosition = require_simple_xpath_position();
    var xpath = _interopRequireWildcard(_simpleXpathPosition);
    function _interopRequireWildcard(obj) {
      if (obj && obj.__esModule) {
        return obj;
      } else {
        var newObj = {};
        if (obj != null) {
          for (var key in obj) {
            if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key];
          }
        }
        newObj["default"] = obj;
        return newObj;
      }
    }
    function _interopRequireDefault(obj) {
      return obj && obj.__esModule ? obj : { "default": obj };
    }
    var SHOW_TEXT = 4;
    function fromRange2(range, root) {
      var sc = range.startContainer;
      var so = range.startOffset;
      var ec = range.endContainer;
      var eo = range.endOffset;
      var start = xpath.fromNode(sc, root);
      var end = xpath.fromNode(ec, root);
      return {
        start,
        end,
        startOffset: so,
        endOffset: eo
      };
    }
    function toRange2(startPath, startOffset, endPath, endOffset, root) {
      var document2 = (0, _getDocument2["default"])(root);
      var sc = xpath.toNode(startPath, root);
      if (sc === null) throw notFound("start");
      var si = document2.createNodeIterator(sc, SHOW_TEXT);
      var so = startOffset - (0, _domSeek2["default"])(si, startOffset);
      sc = si.referenceNode;
      if (!si.pointerBeforeReferenceNode) {
        if (so > 0) throw indexSize("start");
        so += sc.length;
      }
      var ec = xpath.toNode(endPath, root);
      if (ec === null) throw notFound("end");
      var ei = document2.createNodeIterator(ec, SHOW_TEXT);
      var eo = endOffset - (0, _domSeek2["default"])(ei, endOffset);
      ec = ei.referenceNode;
      if (!ei.pointerBeforeReferenceNode) {
        if (eo > 0) throw indexSize("end");
        eo += ec.length;
      }
      var range = document2.createRange();
      range.setStart(sc, so);
      range.setEnd(ec, eo);
      return range;
      function notFound(which) {
        var error = new Error("The " + which + " node was not found.");
        error.name = "NotFoundError";
        return error;
      }
      function indexSize(which) {
        var error = new Error("There is no text at the requested " + which + " offset.");
        error.name = "IndexSizeError";
        return error;
      }
    }
  }
});

// node_modules/xpath-range/index.js
var require_xpath_range = __commonJS({
  "node_modules/xpath-range/index.js"(exports, module) {
    module.exports = require_range();
  }
});

// node_modules/react-selection-highlighter/dist/esm/components/highlighter/Highlighter.js
var import_jsx_runtime3 = __toESM(require_jsx_runtime());
var import_react3 = __toESM(require_react());
var import_client = __toESM(require_client());

// node_modules/react-selection-highlighter/dist/esm/libs/serialize.js
var import_xpath_range = __toESM(require_xpath_range());
var serializeRange = function(range, root) {
  return JSON.stringify((0, import_xpath_range.fromRange)(range, root));
};
var deserializeRange = function(path, root) {
  var parsed = JSON.parse(path);
  try {
    return (0, import_xpath_range.toRange)(parsed.start, parsed.startOffset, parsed.end, parsed.endOffset, root);
  } catch (error) {
    console.error(error);
  }
};

// node_modules/react-selection-highlighter/dist/esm/libs/uid.js
function generateId() {
  return "".concat(Date.now(), "-").concat(Math.random().toString(36).substring(2, 9));
}

// node_modules/react-selection-highlighter/dist/esm/libs/wrapperElements.js
var getSpanElement = function(_a) {
  var className = _a.className;
  var span = document.createElement("span");
  if (className) {
    span.className = className;
  }
  return span;
};
var getPopoverElement = function(_a) {
  var className = _a.className;
  var popover = document.createElement("span");
  if (!className) {
    popover.setAttribute("style", " visibility: hidden;\n        min-width:  20px;\n        background-color: transparent;\n        text-align: center;\n        position: absolute;\n        z-index:  1;\n        bottom:  100%;\n        padding-bottom:16px;\n        opacity:  0;\n        \n        transition: opacity  0.3s;");
    return popover;
  }
  popover.className = className;
  return popover;
};

// node_modules/tslib/tslib.es6.mjs
var __assign = function() {
  __assign = Object.assign || function __assign2(t) {
    for (var s, i = 1, n = arguments.length; i < n; i++) {
      s = arguments[i];
      for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
    }
    return t;
  };
  return __assign.apply(this, arguments);
};
function __awaiter(thisArg, _arguments, P, generator) {
  function adopt(value) {
    return value instanceof P ? value : new P(function(resolve) {
      resolve(value);
    });
  }
  return new (P || (P = Promise))(function(resolve, reject) {
    function fulfilled(value) {
      try {
        step(generator.next(value));
      } catch (e) {
        reject(e);
      }
    }
    function rejected(value) {
      try {
        step(generator["throw"](value));
      } catch (e) {
        reject(e);
      }
    }
    function step(result) {
      result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected);
    }
    step((generator = generator.apply(thisArg, _arguments || [])).next());
  });
}
function __generator(thisArg, body) {
  var _ = { label: 0, sent: function() {
    if (t[0] & 1) throw t[1];
    return t[1];
  }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
  return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() {
    return this;
  }), g;
  function verb(n) {
    return function(v) {
      return step([n, v]);
    };
  }
  function step(op) {
    if (f) throw new TypeError("Generator is already executing.");
    while (g && (g = 0, op[0] && (_ = 0)), _) try {
      if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
      if (y = 0, t) op = [op[0] & 2, t.value];
      switch (op[0]) {
        case 0:
        case 1:
          t = op;
          break;
        case 4:
          _.label++;
          return { value: op[1], done: false };
        case 5:
          _.label++;
          y = op[1];
          op = [0];
          continue;
        case 7:
          op = _.ops.pop();
          _.trys.pop();
          continue;
        default:
          if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) {
            _ = 0;
            continue;
          }
          if (op[0] === 3 && (!t || op[1] > t[0] && op[1] < t[3])) {
            _.label = op[1];
            break;
          }
          if (op[0] === 6 && _.label < t[1]) {
            _.label = t[1];
            t = op;
            break;
          }
          if (t && _.label < t[2]) {
            _.label = t[2];
            _.ops.push(op);
            break;
          }
          if (t[2]) _.ops.pop();
          _.trys.pop();
          continue;
      }
      op = body.call(thisArg, _);
    } catch (e) {
      op = [6, e];
      y = 0;
    } finally {
      f = t = 0;
    }
    if (op[0] & 5) throw op[1];
    return { value: op[0] ? op[1] : void 0, done: true };
  }
}
function __spreadArray(to, from, pack) {
  if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
    if (ar || !(i in from)) {
      if (!ar) ar = Array.prototype.slice.call(from, 0, i);
      ar[i] = from[i];
    }
  }
  return to.concat(ar || Array.prototype.slice.call(from));
}

// node_modules/react-selection-highlighter/dist/esm/components/DeafultPopover.js
var import_jsx_runtime = __toESM(require_jsx_runtime());

// node_modules/react-selection-highlighter/dist/esm/constants/constants.js
var defaultColor = "#15F5BA";
var defaultSelectionWrapperClassName = "bg-lightgreen relative select-none";
var defaultMinSelectionLength = 10;

// node_modules/react-selection-highlighter/dist/esm/components/DeafultPopover.js
var DefaultPopover = function(_a) {
  var selection = _a.selection, removeSelection = _a.removeSelection, updateSelection = _a.updateSelection;
  var handleDelete = function() {
    removeSelection(selection);
  };
  var changeColor = function(colorClassName) {
    var classes = selection.className || defaultSelectionWrapperClassName;
    var classArr = classes.split(" ");
    var colorIndex = classArr.findIndex(function(item) {
      return item.startsWith("bg-");
    });
    if (colorIndex !== -1) {
      classArr.splice(colorIndex, 1);
    }
    classArr.push(colorClassName);
    updateSelection(selection.id, __assign(__assign({}, selection), { className: classArr.join(" ") }));
  };
  return (0, import_jsx_runtime.jsxs)("div", { style: {
    padding: "1rem",
    boxShadow: "5px  5px  10px  0px rgba(0,  0,  0,  0.2)",
    backgroundColor: "white",
    borderRadius: "6px",
    display: "flex",
    flexDirection: "column",
    gap: "5px"
  }, className: "popover", children: [(0, import_jsx_runtime.jsxs)("p", { style: { fontSize: "12px" }, children: [selection.text.length, " characters selected"] }), (0, import_jsx_runtime.jsxs)("div", { style: { display: "flex", minWidth: "150px", gap: "10px", justifyContent: "center", alignItems: "center" }, children: [(0, import_jsx_runtime.jsx)("div", { onClick: function() {
    return changeColor("bg-red");
  }, style: { backgroundColor: "#FF407D", cursor: "pointer", height: "25px", width: "25px", borderRadius: "50%" }, children: " " }), (0, import_jsx_runtime.jsx)("div", { onClick: function() {
    return changeColor("bg-yellow");
  }, style: { backgroundColor: "#F5DD61", cursor: "pointer", height: "25px", width: "25px", borderRadius: "50%" }, children: " " }), (0, import_jsx_runtime.jsx)("div", { onClick: function() {
    return changeColor("bg-blue");
  }, style: { backgroundColor: "#59D5E0", cursor: "pointer", height: "25px", width: "25px", borderRadius: "50%" }, children: " " }), (0, import_jsx_runtime.jsxs)("div", { onClick: handleDelete, style: { color: "red", cursor: "pointer", fontSize: 24, fontWeight: "bold" }, children: [" ", "🗑"] })] })] });
};
var DeafultPopover_default = DefaultPopover;

// node_modules/react-selection-highlighter/dist/esm/hooks/UseSelection.js
var import_react2 = __toESM(require_react());

// node_modules/react-selection-highlighter/dist/esm/providers/SelectionProvider.js
var import_jsx_runtime2 = __toESM(require_jsx_runtime());
var import_react = __toESM(require_react());
var SelectionsContext = (0, import_react.createContext)(null);
var SelectionProvider = function(_a) {
  var children = _a.children;
  var _b = (0, import_react.useState)([]), selections = _b[0], setSelections = _b[1];
  var value = { selections, setSelections };
  return (0, import_jsx_runtime2.jsx)(SelectionsContext.Provider, { value, children });
};

// node_modules/react-selection-highlighter/dist/esm/hooks/UseSelection.js
var useSelections = function() {
  var selectionContext = (0, import_react2.useContext)(SelectionsContext);
  if (!selectionContext) {
    throw new Error("useSelection hook must be used inside selectionProvider");
  }
  var selections = selectionContext.selections, setSelections = selectionContext.setSelections;
  var addSelection = function(selection) {
    return __awaiter(void 0, void 0, void 0, function() {
      return __generator(this, function(_a) {
        setSelections(function(prev) {
          var index = prev.findIndex(function(item) {
            return item.id === selection.id;
          });
          if (index === -1) {
            return __spreadArray(__spreadArray([], prev, true), [selection], false);
          }
          return prev;
        });
        return [
          2
          /*return*/
        ];
      });
    });
  };
  var updateSelection = function(id, updatedSelection) {
    return __awaiter(void 0, void 0, void 0, function() {
      return __generator(this, function(_a) {
        setSelections(function(prev) {
          var index = prev.findIndex(function(item) {
            return item.id === id;
          });
          if (index !== -1) {
            prev.splice(index, 1);
          }
          return __spreadArray(__spreadArray([], prev, true), [updatedSelection], false);
        });
        return [
          2
          /*return*/
        ];
      });
    });
  };
  var removeSelection = function(selection) {
    setSelections(function(prev) {
      var index = prev.findIndex(function(item) {
        return item.id === selection.id;
      });
      if (index !== -1) {
        prev = prev.filter(function(item) {
          return item.id !== selection.id;
        });
      }
      return __spreadArray([], prev, true);
    });
  };
  return { selections, setSelections, addSelection, updateSelection, removeSelection };
};

// node_modules/react-selection-highlighter/dist/esm/libs/dom.js
var addHighlight = function(range, element) {
  var _a;
  if (!isHighlightable(range)) {
    return;
  }
  var exists = document.getElementById(element.id);
  if (exists) {
    exists.className = element.className;
    return;
  }
  element.appendChild(range.extractContents());
  range.insertNode(element);
  (_a = window.getSelection()) === null || _a === void 0 ? void 0 : _a.removeAllRanges();
};
var isHighlightable = function(range) {
  var contents = range.cloneContents();
  var hasp = [];
  contents.childNodes.forEach(function(item) {
    if (item.nodeName === "P") {
      hasp.push(true);
    } else {
      hasp.push(false);
    }
  });
  return !hasp.includes(true);
};

// node_modules/react-selection-highlighter/dist/esm/libs/createRange.js
function findContainerByText(text, root) {
  var _a;
  var walker = document.createTreeWalker(root, NodeFilter.SHOW_TEXT, null);
  var node;
  while (node = walker.nextNode()) {
    if ((_a = node.nodeValue) === null || _a === void 0 ? void 0 : _a.trim().includes(text.trim())) {
      return node;
    }
  }
  return null;
}
function getNextNode(node) {
  if (!node) {
    return null;
  }
  if (node.firstChild)
    return node.firstChild;
  while (node) {
    if (node.nextSibling)
      return node.nextSibling;
    node = node.parentNode;
  }
}
function getNodeCount(range) {
  var count = 1;
  var node = range.startContainer;
  while (node && node !== range.endContainer) {
    if (node.nodeType === Node.ELEMENT_NODE) {
      node = getNextNode(node);
      continue;
    }
    count++;
    node = getNextNode(node);
  }
  return count;
}
var getRangeStartEndContainerText = function(range) {
  var _a, _b;
  if (range.startContainer === range.endContainer) {
    return { startContainerText: range.toString(), endContainerText: range.toString() };
  }
  var startContainerText = (_a = range.startContainer.textContent) === null || _a === void 0 ? void 0 : _a.substring(range.startOffset, range.startContainer.textContent.length);
  var endContainerText = (_b = range.endContainer.textContent) === null || _b === void 0 ? void 0 : _b.substring(0, range.endOffset);
  return { startContainerText, endContainerText };
};
function getOriginalRange(range, root) {
  var _a, _b, _c, _d, _e;
  var firstOffset = 0, secondOffset = 0;
  var firstContainer = findContainerByText(range.startContainer.textContent, root);
  var lastContainter = findContainerByText(range.endContainer.textContent, root);
  var rangeNodeCount = getNodeCount(range);
  if (rangeNodeCount === 1) {
    var rangeFirstContainerText = (_a = range.startContainer.textContent) === null || _a === void 0 ? void 0 : _a.substring(range.startOffset, range.endOffset);
    firstOffset = ((_b = firstContainer === null || firstContainer === void 0 ? void 0 : firstContainer.textContent) === null || _b === void 0 ? void 0 : _b.indexOf(rangeFirstContainerText)) || 0;
    secondOffset = firstOffset + rangeFirstContainerText.length;
  } else {
    var rangeFirstContainerText = (_c = range.startContainer.textContent) === null || _c === void 0 ? void 0 : _c.substring(range.startOffset, range.startContainer.textContent.length);
    firstOffset = ((_d = firstContainer === null || firstContainer === void 0 ? void 0 : firstContainer.textContent) === null || _d === void 0 ? void 0 : _d.indexOf(rangeFirstContainerText)) || 0;
    var rangeLastContainerText = (_e = range.endContainer.textContent) === null || _e === void 0 ? void 0 : _e.substring(0, range.endOffset);
    secondOffset = (rangeLastContainerText === null || rangeLastContainerText === void 0 ? void 0 : rangeLastContainerText.length) || 0;
  }
  if (!firstContainer || !lastContainter) {
    return null;
  }
  var newRange = document.createRange();
  newRange.setStart(firstContainer, firstOffset);
  newRange.setEnd(lastContainter, secondOffset);
  return newRange;
}

// node_modules/react-selection-highlighter/dist/esm/libs/sort.js
function extractNumbers(str) {
  var cleanedStr = str.replace(/h\d/g, "");
  var regex = /\d+/g;
  var matches = cleanedStr.match(regex);
  if (matches) {
    if (matches.length < 2) {
      matches[0] = matches[0].concat("11");
    }
    return +matches.join("");
  } else {
    return 0;
  }
}
var sortByPositionAndOffset = function(slections) {
  return slections.sort(function(a, b) {
    var path1 = JSON.parse(a.meta);
    var path2 = JSON.parse(b.meta);
    var path1Weight = extractNumbers(path1.start) * 1e4 + path1.startOffset;
    var path2Weight = extractNumbers(path2.start) * 1e4 + path2.startOffset;
    return path2Weight - path1Weight;
  });
};

// node_modules/react-selection-highlighter/dist/esm/components/highlighter/Highlighter.js
var Highlighter = function(_a) {
  var htmlString = _a.htmlString, onClickHighlight = _a.onClickHighlight, disablePopover = _a.disablePopover, maxSelectionLength = _a.maxSelectionLength, minSelectionLength = _a.minSelectionLength, className = _a.className, PopoverChildren = _a.PopoverChildren, PopoverClassName = _a.PopoverClassName, selectionWrapperClassName = _a.selectionWrapperClassName, onSelection = _a.onSelection, onClick = _a.onClick;
  var _b = useSelections(), selections = _b.selections, addSelection = _b.addSelection, removeSelection = _b.removeSelection, updateSelection = _b.updateSelection;
  var rootRef = (0, import_react3.useRef)(null);
  var tempRef = (0, import_react3.useRef)(null);
  var div = document.createElement("div");
  tempRef.current = div;
  tempRef.current.innerHTML = htmlString;
  var getWrapper = (0, import_react3.useCallback)(function(selection) {
    var span = getSpanElement({ className: selection.className || defaultSelectionWrapperClassName });
    if (!disablePopover) {
      var popover_1 = getPopoverElement({ className: PopoverClassName });
      if (!PopoverClassName) {
        span.onmouseover = function() {
          popover_1.style.visibility = "visible";
          popover_1.style.opacity = "1";
        };
        span.onmouseout = function() {
          popover_1.style.visibility = "hidden";
          popover_1.style.opacity = "0";
        };
      }
      popover_1.id = "pop-".concat(selection.id);
      span.appendChild(popover_1);
    }
    if (onClickHighlight) {
      span.onclick = function(e) {
        return onClickHighlight(selection, e);
      };
    }
    span.id = selection.id;
    return span;
  }, [PopoverClassName, disablePopover, onClickHighlight]);
  var handleMouseUp = function() {
    var selection = window.getSelection();
    if (!selection)
      return;
    if (!minSelectionLength) {
      minSelectionLength = defaultMinSelectionLength;
    }
    if (minSelectionLength && selection.toString().length < minSelectionLength)
      return;
    if (maxSelectionLength && selection.toString().length > maxSelectionLength)
      return;
    var range = selection.getRangeAt(0);
    if (!isHighlightable(range))
      return;
    var expRange = getOriginalRange(range, tempRef.current);
    if (!expRange)
      return;
    var _a2 = getRangeStartEndContainerText(range), startContainerText = _a2.startContainerText, endContainerText = _a2.endContainerText;
    var newSelection = {
      meta: serializeRange(expRange, tempRef.current),
      text: range.toString(),
      id: "selection-".concat(generateId()),
      className: selectionWrapperClassName || defaultSelectionWrapperClassName,
      startContainerText,
      endContainerText
    };
    addSelection(newSelection);
    onSelection && onSelection(newSelection);
  };
  (0, import_react3.useEffect)(function() {
    var sortedSelections = sortByPositionAndOffset(selections);
    if (!rootRef.current)
      return;
    rootRef.current.innerHTML = "";
    rootRef.current.innerHTML = htmlString;
    if (sortedSelections && sortedSelections.length) {
      for (var i = 0; i < sortedSelections.length; i++) {
        var item = sortedSelections[i];
        var range = deserializeRange(item.meta, rootRef.current);
        if (range) {
          addHighlight(range, getWrapper(item));
        }
        var popoverRoot = document.getElementById("pop-".concat(item.id));
        if (!popoverRoot)
          return;
        var root = import_client.default.createRoot(popoverRoot);
        if (PopoverChildren) {
          root.render((0, import_jsx_runtime3.jsx)(PopoverChildren, { selection: item, removeSelection, updateSelection }));
        } else {
          root.render((0, import_jsx_runtime3.jsx)(DeafultPopover_default, { removeSelection, selection: item, updateSelection }));
        }
      }
    }
  }, [selections, getWrapper, PopoverChildren, htmlString, removeSelection, updateSelection]);
  return (0, import_jsx_runtime3.jsx)("div", { ref: rootRef, id: "highlighter-root", onClick, onMouseUp: handleMouseUp, className });
};
export {
  Highlighter,
  SelectionProvider,
  SelectionsContext,
  defaultColor,
  defaultMinSelectionLength,
  defaultSelectionWrapperClassName,
  useSelections
};
//# sourceMappingURL=react-selection-highlighter.js.map
